apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend-svc
spec:
  selector:
    app: backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5678
  type: {{ .Values.service.type }}