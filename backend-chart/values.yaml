replicaCount: 2

image:
  repository: hashicorp/http-echo
  tag: latest

echoText: "Hello from Helm-managed backend"

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 200m
    memory: 64Mi