apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-database-svc
spec:
  selector:
    app: database
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
  type: {{ .Values.service.type }}